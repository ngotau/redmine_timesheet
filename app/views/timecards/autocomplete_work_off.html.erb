<%
  status_str = (@this_work.work_status == 1) ? l(:ts_status_on) : l(:ts_status_off)
  output_str = (@error_message != nil) ? "NG|#{@error_message}": "OK|#{status_str},#{@old_hours},#{@this_work.work_hours},#{@this_work.work_lunch}" 
%>
<%=output_str%>
